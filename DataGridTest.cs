using Avalonia.Controls;

namespace AssistenciaTecnicaApp
{
    // Apenas um arquivo simples para testar o namespace do DataGrid
    public class DataGridTest
    {
        public void TestDataGrid()
        {
            // Verifica se o DataGrid está acessível
            DataGrid grid = new DataGrid();
        }
    }
} 